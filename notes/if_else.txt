if kondisi: # dianggap 1
    aksi
else: # dianggap 0
    aksi_lain
Kalau kondisi = True → blok if jalan
Kalau kondisi = False → blok else jalan

= untuk vairabel
== untuk membandingkan dengan variabel

.strip() → hapus spasi di depan/belakang
.lower() → ubah jadi huruf kecil semua

#1 Cek bilangan ganjil atau genap
angka = int (input ("Masukkan angka yang kamu suka: "))
if angka % 2 == 0: kategori = "bilangan genap."
else: kategori = "bilangan ganjil."
print (angka, "Termasuk kategori", kategori)

#2 Login
username_benar = "admin"
password_benar = "1234"
username_user = input ("Input username: ")
password_user = input ("Input password: ")

if username_user == username_benar and password_user == password_benar: print ("Login Berhasil")
else: print ("Login Gagal")

#3 Grade Nilai
nilai = int (input ("Input nilai anda: "))
if nilai >= 90: print ("A")
elif nilai >= 80: print ("B")
elif nilai >= 70: print ("C")
else: print ("D")

#4 Kalkulator Sederhana
angka1 = int(input ("Angka pertama: "))
operator = input ("Masukkan operator:")
angka2 = int(input ("Angka kedua: "))
if operator == "+" :
    hasil = angka1 + angka2
elif operator == "-" : hasil = angka1 - angka2
elif operator == "*" : hasil = angka1 * angka2
elif operator == "/" : hasil = angka1 / angka2
else: hasil = "Operator tidak dikenali"
print ("Hasil:", hasil)
